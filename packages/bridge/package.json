{
  "name": "qing-bridge",
  "version": "0.1.0",
  "description": "用于获取并解析网站数据",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "dev": "tsup --watch",
    "build": "tsup",
    "example": "concurrently \"tsup -d example --watch --clean false\" \"serve example\"",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "license": "MIT"
}
